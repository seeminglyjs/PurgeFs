# P4 TUI(대화형 선택) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**목표:** `purgefs purge -i` 로 대화형 TUI를 띄워, 분류된 junk 카테고리를 체크박스로 고르고(용량 막대·선택 합계 표시) 엔터로 정리한다. 취소하면 아무것도 삭제하지 않는다.

**아키텍처:** 새 `internal/tui` 패키지가 bubbletea `Model`(선택 상태·키 처리·렌더)을 제공한다. 순수한 선택 로직(`New`/`Update`/`SelectedPaths`/`SelectedSize`)은 단위 테스트하고, 실제 터미널 프로그램(`tea.NewProgram(...).Run()`)은 `cmd` 에서 얇게 감싼다. `cmd/purge.go` 는 `-i/--interactive` 플래그가 있으면 분류 결과를 TUI 항목으로 바꿔 띄우고, 선택된 경로를 기존 `trash.Trasher` 로 처리한다.

**기술 스택:** Go, `cobra`, **신규: `github.com/charmbracelet/bubbletea`**(Go TUI 사실상 표준). 엔진은 표준 라이브러리만 유지.

## 전역 제약 (모든 태스크에 적용)

- Go 모듈 `github.com/seeminglyjs/PurgeFs`; `go.mod` 하한 `go 1.24`.
- `internal/engine`·`internal/trash` 는 그대로 두고 수정하지 않는다.
- bubbletea 의존성은 `internal/tui` 와 `cmd` 에만 둔다. 엔진은 여전히 표준 라이브러리만. `go.mod`/`go.sum` 은 커밋한다.
- **코드 주석은 한국어**, **문서는 한국어**(코드 블록·셸 명령·식별자·에러 문자열·커밋 메시지는 그대로).
- 커밋에 Claude / 어떤 AI 도 co-author·트레일러로 넣지 않는다. Conventional-commit 접두어.
- 삭제는 위험하다. TUI 취소(q/esc) 시 아무것도 삭제하지 않는다. 선택 0개면 삭제하지 않는다.

## 설계 판단

- 선택 단위는 **카테고리**(node_modules, build-cache, ...). 개별 파일 선택은 나중(P4 이후). `engine.Classify` 결과를 그대로 항목으로 쓴다.
- `tui.Model` 은 용량 포맷 함수를 주입받는다(`format func(int64) string`). 그래서 `internal/tui` 는 `cmd` 의 `humanBytes` 를 import 하지 않아도 되고, DRY 를 위해 humanBytes 를 옮기지 않는다.
- `tea.NewProgram(...).Run()` 은 터미널이 필요해 단위 테스트하지 않는다. 대신 모델의 `Update`/선택 로직과 `groupsToItems` 헬퍼를 테스트한다.
- 기본(`purge`)은 P3 동작 그대로. TUI 는 `-i/--interactive` 옵트인. tty 자동 감지로 기본화하는 것은 나중 폴리시.

## 파일 구조

- 생성 `internal/tui/model.go` — `Item`, `Model`, `New`, `Init`/`Update`/`View`, `SelectedPaths`/`SelectedSize`/`Confirmed`, `sizeBar`.
- 생성 `internal/tui/model_test.go`.
- 수정 `cmd/purge.go` — `-i/--interactive` 플래그, `groupsToItems` 헬퍼, `reportTrashResult` 헬퍼, `runPurgeInteractive`.
- 수정 `cmd/purge_test.go` — `groupsToItems` 테스트.
- 수정 `go.mod`/`go.sum` — bubbletea 추가.

---

### Task 1: TUI 모델(internal/tui)

**파일:**
- 생성: `internal/tui/model.go`
- 테스트: `internal/tui/model_test.go`

**인터페이스:**
- 소비: `github.com/charmbracelet/bubbletea`.
- 생산:
  - `type Item struct { Label string; Size int64; Paths []string }`
  - `func New(items []Item, format func(int64) string) Model`
  - `Model` 은 `tea.Model`(`Init`/`Update`/`View`) 구현.
  - `func (m Model) SelectedPaths() []string`, `func (m Model) SelectedSize() int64`, `func (m Model) Confirmed() bool`

- [ ] **단계 1: bubbletea 의존성 추가**

실행:
```bash
export PATH="/opt/homebrew/bin:$PATH"
go get github.com/charmbracelet/bubbletea@latest
```
기대: `go.mod` 에 `github.com/charmbracelet/bubbletea` 추가, `go.sum` 갱신.

- [ ] **단계 2: 실패 테스트 작성**

`internal/tui/model_test.go` 생성:

```go
package tui

import (
	"testing"

	tea "github.com/charmbracelet/bubbletea"
)

func idFmt(n int64) string { return "" } // 테스트는 크기 문자열을 검사하지 않는다

func sampleItems() []Item {
	return []Item{
		{Label: "node_modules", Size: 1000, Paths: []string{"/p/node_modules"}},
		{Label: "os-junk", Size: 6, Paths: []string{"/p/.DS_Store"}},
	}
}

func key(t tea.KeyType) tea.KeyMsg { return tea.KeyMsg{Type: t} }
func rune_(r rune) tea.KeyMsg      { return tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{r}} }

func TestNewSelectsAll(t *testing.T) {
	m := New(sampleItems(), idFmt)
	if got := len(m.SelectedPaths()); got != 2 {
		t.Errorf("SelectedPaths = %d, want 2 (all selected by default)", got)
	}
	if m.SelectedSize() != 1006 {
		t.Errorf("SelectedSize = %d, want 1006", m.SelectedSize())
	}
}

func TestSpaceTogglesSelection(t *testing.T) {
	m := New(sampleItems(), idFmt)
	// 커서는 0(node_modules)에서 시작. 스페이스로 해제.
	updated, _ := m.Update(key(tea.KeySpace))
	m = updated.(Model)
	if m.SelectedSize() != 6 {
		t.Errorf("after deselecting node_modules, SelectedSize = %d, want 6", m.SelectedSize())
	}
	paths := m.SelectedPaths()
	if len(paths) != 1 || paths[0] != "/p/.DS_Store" {
		t.Errorf("SelectedPaths = %v, want [/p/.DS_Store]", paths)
	}
}

func TestCursorDownThenToggle(t *testing.T) {
	m := New(sampleItems(), idFmt)
	updated, _ := m.Update(key(tea.KeyDown)) // 커서 1(os-junk)로
	m = updated.(Model)
	updated, _ = m.Update(key(tea.KeySpace)) // os-junk 해제
	m = updated.(Model)
	if m.SelectedSize() != 1000 {
		t.Errorf("SelectedSize = %d, want 1000", m.SelectedSize())
	}
}

func TestEnterConfirmsAndQuits(t *testing.T) {
	m := New(sampleItems(), idFmt)
	updated, cmd := m.Update(key(tea.KeyEnter))
	m = updated.(Model)
	if !m.Confirmed() {
		t.Error("enter must set Confirmed")
	}
	if cmd == nil {
		t.Fatal("enter must return a quit command")
	}
	if _, ok := cmd().(tea.QuitMsg); !ok {
		t.Error("enter's command must be tea.Quit")
	}
}

func TestQuitCancels(t *testing.T) {
	m := New(sampleItems(), idFmt)
	updated, cmd := m.Update(rune_('q'))
	m = updated.(Model)
	if m.Confirmed() {
		t.Error("q must not confirm")
	}
	if cmd == nil {
		t.Fatal("q must return a quit command")
	}
	if _, ok := cmd().(tea.QuitMsg); !ok {
		t.Error("q's command must be tea.Quit")
	}
}
```

- [ ] **단계 3: 테스트 실행해 실패 확인**

실행: `go test ./internal/tui/ -v`
기대: FAIL — `undefined: New` / `undefined: Item`.

- [ ] **단계 4: 구현 작성**

`internal/tui/model.go` 생성 (주석 한국어):

```go
// Package tui 는 정리 대상을 대화형으로 고르는 bubbletea 화면을 제공한다. 선택 로직은
// 순수 함수로 두어 터미널 없이 테스트할 수 있다.
package tui

import (
	"fmt"
	"strings"

	tea "github.com/charmbracelet/bubbletea"
)

// Item 은 정리 대상 한 묶음(카테고리)이다.
type Item struct {
	Label string
	Size  int64
	Paths []string
}

// Model 은 선택 화면의 상태다.
type Model struct {
	items     []Item
	cursor    int
	selected  map[int]bool
	format    func(int64) string
	confirmed bool
	quit      bool
}

// New 는 모든 항목이 선택된 상태로 시작하는 Model 을 만든다. format 은 용량을 사람이 읽는
// 문자열로 바꾼다.
func New(items []Item, format func(int64) string) Model {
	sel := make(map[int]bool, len(items))
	for i := range items {
		sel[i] = true
	}
	return Model{items: items, selected: sel, format: format}
}

// Init 은 초기 명령이 없다.
func (m Model) Init() tea.Cmd { return nil }

// Update 는 키 입력에 따라 커서 이동·토글·확정·취소를 처리한다.
func (m Model) Update(msg tea.Msg) (tea.Model, tea.Cmd) {
	key, ok := msg.(tea.KeyMsg)
	if !ok {
		return m, nil
	}
	switch key.String() {
	case "up", "k":
		if m.cursor > 0 {
			m.cursor--
		}
	case "down", "j":
		if m.cursor < len(m.items)-1 {
			m.cursor++
		}
	case " ":
		m.selected[m.cursor] = !m.selected[m.cursor]
	case "enter":
		m.confirmed = true
		return m, tea.Quit
	case "q", "esc", "ctrl+c":
		m.quit = true
		return m, tea.Quit
	}
	return m, nil
}

// View 는 항목 목록·체크·용량 막대·선택 합계를 그린다.
func (m Model) View() string {
	if m.quit || m.confirmed {
		return ""
	}
	var b strings.Builder
	b.WriteString("정리할 항목 선택 (↑/↓ 이동, space 토글, enter 실행, q 취소)\n\n")

	var maxSize int64 = 1
	for _, it := range m.items {
		if it.Size > maxSize {
			maxSize = it.Size
		}
	}
	for i, it := range m.items {
		cursor := " "
		if i == m.cursor {
			cursor = ">"
		}
		check := " "
		if m.selected[i] {
			check = "x"
		}
		fmt.Fprintf(&b, "%s [%s] %-14s %10s %s\n",
			cursor, check, it.Label, m.format(it.Size), sizeBar(it.Size, maxSize, 20))
	}
	fmt.Fprintf(&b, "\n선택 합계: %s\n", m.format(m.SelectedSize()))
	return b.String()
}

// SelectedSize 는 선택된 항목 크기의 합이다.
func (m Model) SelectedSize() int64 {
	var total int64
	for i, it := range m.items {
		if m.selected[i] {
			total += it.Size
		}
	}
	return total
}

// SelectedPaths 는 선택된 항목들의 경로를 평탄화해 반환한다.
func (m Model) SelectedPaths() []string {
	var paths []string
	for i, it := range m.items {
		if m.selected[i] {
			paths = append(paths, it.Paths...)
		}
	}
	return paths
}

// Confirmed 는 사용자가 enter 로 실행을 확정했는지 여부다.
func (m Model) Confirmed() bool { return m.confirmed }

// sizeBar 는 size/max 비율을 width 칸의 막대로 그린다.
func sizeBar(size, max int64, width int) string {
	if max <= 0 {
		return ""
	}
	n := int(int64(width) * size / max)
	if n < 0 {
		n = 0
	}
	if n > width {
		n = width
	}
	return strings.Repeat("█", n) + strings.Repeat("░", width-n)
}
```

- [ ] **단계 5: 테스트 실행해 통과 확인**

실행: `go test ./internal/tui/ -v`
기대: PASS.

- [ ] **단계 6: 커밋**

```bash
git add internal/tui/model.go internal/tui/model_test.go go.mod go.sum
git commit -m "feat: add interactive selection TUI model (bubbletea)"
```

---

### Task 2: purge 대화형 배선(-i)

**파일:**
- 수정: `cmd/purge.go`
- 수정: `cmd/purge_test.go`

**인터페이스:**
- 소비: `engine.Scan`/`Classify`/`DefaultRules`/`CategoryGroup`; `trash.Trasher`/`Result`; `tui.New`/`tui.Item`/`tui.Model`; `humanBytes`; `github.com/charmbracelet/bubbletea`.
- 생산:
  - `func groupsToItems(groups []engine.CategoryGroup) []tui.Item`
  - `func reportTrashResult(w io.Writer, res trash.Result) error` — 처리·실패 개수와 첫 실패 이유를 출력하고, 하나도 처리 못 했으면 에러 반환.
  - `func runPurgeInteractive(w io.Writer, path string, tr trash.Trasher) error`
  - `purgeCmd` 에 `-i/--interactive` 플래그 추가.

- [ ] **단계 1: 실패 테스트 작성**

`cmd/purge_test.go` 에 추가:

```go
func TestGroupsToItems(t *testing.T) {
	groups := []engine.CategoryGroup{
		{Category: "node_modules", Size: 1000, Count: 1, Paths: []string{"/p/node_modules"}},
		{Category: "os-junk", Size: 6, Count: 2, Paths: []string{"/p/.DS_Store", "/p/x/.DS_Store"}},
	}
	items := groupsToItems(groups)
	if len(items) != 2 {
		t.Fatalf("items = %d, want 2", len(items))
	}
	if items[0].Label != "node_modules" || items[0].Size != 1000 || len(items[0].Paths) != 1 {
		t.Errorf("item[0] = %+v", items[0])
	}
	if items[1].Label != "os-junk" || len(items[1].Paths) != 2 {
		t.Errorf("item[1] = %+v", items[1])
	}
}
```

(`engine` 은 `cmd/purge_test.go` 상단 import 에 `"github.com/seeminglyjs/PurgeFs/internal/engine"` 를 추가한다.)

- [ ] **단계 2: 테스트 실행해 실패 확인**

실행: `go test ./cmd/ -run TestGroupsToItems -v`
기대: FAIL — `undefined: groupsToItems`.

- [ ] **단계 3: purge.go 에 헬퍼·대화형 경로·플래그 추가**

(1) import 에 `github.com/seeminglyjs/PurgeFs/internal/tui` 와 `tea "github.com/charmbracelet/bubbletea"` 를 추가한다.

(2) `runPurge` 의 결과 출력 꼬리(현재 `res := tr.Trash(paths)` 부터 마지막 `return nil` 까지)를 `reportTrashResult` 호출로 바꾼다. 즉 `runPurge` 안을:

```go
	res := tr.Trash(paths)
	return reportTrashResult(w, res)
```
로 끝내고, 아래 헬퍼를 추가한다(출력 문구는 기존과 동일하게 유지):

```go
// reportTrashResult 는 처리·실패 개수를 출력한다. 실패가 있으면 첫 이유를 보여주고, 하나도
// 처리 못 했으면 스크립트가 감지하도록 에러를 반환한다.
func reportTrashResult(w io.Writer, res trash.Result) error {
	fmt.Fprintf(w, "처리 %d개", len(res.Trashed))
	if len(res.Failed) > 0 {
		fmt.Fprintf(w, ", 실패 %d개", len(res.Failed))
	}
	fmt.Fprintln(w)
	if len(res.Failed) > 0 {
		fmt.Fprintf(w, "  첫 실패: %s: %v\n", res.Failed[0].Path, res.Failed[0].Err)
		if len(res.Trashed) == 0 {
			return fmt.Errorf("purge failed: %d개 항목 모두 실패", len(res.Failed))
		}
	}
	return nil
}
```

(3) `groupsToItems` 와 `runPurgeInteractive` 를 추가한다:

```go
// groupsToItems 는 분류 결과를 TUI 항목으로 바꾼다.
func groupsToItems(groups []engine.CategoryGroup) []tui.Item {
	items := make([]tui.Item, 0, len(groups))
	for _, g := range groups {
		items = append(items, tui.Item{Label: g.Category, Size: g.Size, Paths: g.Paths})
	}
	return items
}

// runPurgeInteractive 는 분류 결과를 TUI 로 띄워 사용자가 고른 항목만 tr 로 처리한다. tty 가
// 필요한 tea.Program 실행이라 단위 테스트하지 않는다(선택 로직은 internal/tui 에서 테스트).
func runPurgeInteractive(w io.Writer, path string, tr trash.Trasher) error {
	report, _, err := engine.Scan(path)
	if err != nil {
		return err
	}
	groups := engine.Classify(report, engine.DefaultRules())
	if len(groups) == 0 {
		fmt.Fprintln(w, "정리할 junk가 없습니다.")
		return nil
	}

	final, err := tea.NewProgram(tui.New(groupsToItems(groups), humanBytes)).Run()
	if err != nil {
		return err
	}
	m, ok := final.(tui.Model)
	if !ok || !m.Confirmed() {
		fmt.Fprintln(w, "취소했습니다.")
		return nil
	}
	paths := m.SelectedPaths()
	if len(paths) == 0 {
		fmt.Fprintln(w, "선택한 항목이 없습니다.")
		return nil
	}
	return reportTrashResult(w, tr.Trash(paths))
}
```

(4) `purgeCmd.RunE` 에서 대화형 분기를 추가한다. `guardRoot` 통과·`tr` 선택 뒤, `runPurge` 호출 자리를 다음으로 바꾼다:

```go
		if purgeInteractive {
			return runPurgeInteractive(cmd.OutOrStdout(), path, tr)
		}
		return runPurge(cmd.OutOrStdout(), cmd.InOrStdin(), path, tr, purgeHard, purgeYes)
```

(5) 플래그 변수와 등록을 추가한다. 파일 상단 플래그 변수에 `purgeInteractive` 를 더하고, `init` 에:

```go
	purgeCmd.Flags().BoolVarP(&purgeInteractive, "interactive", "i", false, "대화형 TUI로 선택해 정리")
```
를 추가한다(`purgeInteractive bool` 변수 선언 포함).

- [ ] **단계 4: 테스트 실행해 통과 확인**

실행: `go test ./cmd/ -run TestGroupsToItems -v`
기대: PASS. 기존 `TestRunPurge*`·`TestGuardRoot*` 도 여전히 통과해야 한다(`runPurge` 출력 문구 불변).

- [ ] **단계 5: 전체 빌드 + 테스트 + 수동 확인**

실행:
```bash
go build -o purgefs . && go vet ./... && go test ./...
mkdir -p /tmp/pf-demo/node_modules && echo x > /tmp/pf-demo/.DS_Store && head -c 2048 /dev/zero > /tmp/pf-demo/node_modules/a.bin
./purgefs purge /tmp/pf-demo -i     # TUI 뜸: space 토글, enter 실행 또는 q 취소
rm -rf /tmp/pf-demo purgefs
```
기대: 빌드 OK; vet 클린; 전체 테스트 PASS; `-i` 로 TUI 가 뜨고 화살표/space/enter/q 가 동작.

- [ ] **단계 6: 커밋**

```bash
git add cmd/purge.go cmd/purge_test.go
git commit -m "feat: add interactive purge (-i) driving the selection TUI"
```

---

## 자체 검토

**1. 스펙 커버리지 (P4 범위):**
- bubbletea 선택 UI(체크박스·용량 막대·선택 합계) → Task 1 `Model`/`View`/`sizeBar`. ✓
- 확인/취소 → Task 1 `enter`(Confirmed)·`q/esc`(취소). ✓
- 대화형 purge 연결 → Task 2 `-i`·`runPurgeInteractive`. ✓
- 설계상 P4 범위 밖: 카테고리↔트리 뷰 토글(개별 파일 선택), staleness 표시, undo/프리셋(P5).

**2. Placeholder 스캔:** TBD/TODO 없음. `tea.Program.Run` 미검증은 의도된 얇은 I/O 경계이며 이유를 명시. ✓

**3. 타입 일관성:** `tui.Item{Label,Size,Paths}`, `tui.New(items, func(int64)string) Model`, `Model.SelectedPaths()/SelectedSize()/Confirmed()`, `groupsToItems([]engine.CategoryGroup) []tui.Item`, `reportTrashResult(io.Writer, trash.Result) error` 가 태스크·테스트에서 동일. `humanBytes` 시그니처(`func(int64) string`)가 `tui.New` 의 `format` 파라미터와 일치. ✓

## 참고

- `internal/tui` 는 bubbletea 에 의존하지만 `internal/engine` 은 그대로 표준 라이브러리만 유지한다.
- `--hard` 와 `-i` 를 함께 쓰면 TUI 로 고른 항목을 완전 삭제한다. TUI 에는 아직 휴지통/완전삭제 구분 표시가 없다 — P4 이후 헤더에 표기 고려.
- tty 자동 감지로 `purge` 기본을 TUI 로 만드는 것은 나중 폴리시. 지금은 `-i` 옵트인.
- P5(undo/프리셋)에서 `trash.Result` 에 원본→휴지통 경로 매핑을 추가하면 복원이 쉬워진다(P3 최종 리뷰 메모).
